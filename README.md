Create a list of observations of each variable.
Name the elements of the list based on the feature names (from "features.txt")
Create a data frame with columns: activity, subject, variable, measurement